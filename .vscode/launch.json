{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Canvas",
            "url": "http://localhost:5501",
            "webRoot": "${workspaceFolder}"
        }
    ]
}